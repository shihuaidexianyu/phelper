# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository status

**M0 (read-only probe), M1 (telemetry engine), M2 (control foundation), M3 (power-limits special: PERFEPP1 + 0x22 + 0x29), M4.1 (PL4 readback channel + 0x29 byte2 in the control plane), M5 (profiles — §36 landed, Phase 4 pulled forward), M6 (GPUI shell v0.1 — §58 Phase 3), v0.2 (render-performance pass), and the Windows PPM fine-policy pass are complete and verified on the reference machine.** Workspace: `phelper-domain` (pure domain model + ports) → `phelper-core` (engine + platform adapters + Application layer) → `phelper-cli` (dev/verification harness) + `apps/desktop` (GPUI shell).

M1 delivered: `Engine` (telemetry-only assembly, provider-tolerant startup), `TelemetryCoordinator` thread with per-cadence collectors (PawnIO 250 ms / NVAPI 500 ms / PDH 1 s / HP fans 1 s hard rule / power 5 s), bounded ring-buffer store (8192/metric), `TelemetryHandle` {snapshot/history/stats/subscribe/request_fresh}, and the HpActor thread owning all WMI traffic (closed typed-request enum — no raw-payload channel, §50). The coordinator thread is **pinned to logical processor 0** (APERF/MPERF are per-core MSRs; same-core reads make the effective-clock ratio honest — measured starvation without pinning). Verified on-device: all metrics live over a 10-min load-phased run, jitter ≤35 ms normal, graceful shutdown. **GPU power = NVML** (`nvmlDeviceGetPowerUsage` — on-device evidence overturned the R5 "NVML dead on AD107" finding; ClientPowerTopology reports 0 entries on this machine and is now the declared fallback).

M2 delivered: **`ControlCoordinator`** (single-writer FIFO thread, sync_channel(32)→Busy; §32 multi-step CPU policy; AR-10 verification: PPM readback=Verified / fan 0x2D 8×1s ±1000 RPM / thermal+max-fan=TrustedNoReadback), **`SafetySupervisor`** (write-time validation incl. fresh-temp ≤5 s gate for manual fan; ≥90 °C→ForceMaxFan / ≤85 °C→ReleaseTo hysteresis while fan-held; 90 s sensor-freeze watchdog → restore auto), **`KeepAliveService`** (60 s 0x10 heartbeat + non-default TrustedWrite re-assertion; 2 consecutive failures → fail closed), **`ControlJournal`** (JSONL, origin user/keepalive/safety/shutdown, before/after evidence per step), capability probe THROUGH the running HpActor (`probe_runtime` — never a second WMI connection), OGH second-writer startup scan (warn-only), readback metrics (`cpu.epp_ac/dc`, `cpu.pl1_w/pl2_w/power_limit_raw` via MSR 0x610, `gpu.power_limit_w` via NVML **GetEnforcedPowerLimit** — the Management variant is NOT_SUPPORTED here), and `phelper-cli control` subcommands with §56 before/command/after output. First writes: EPP / max-freq / boost (PowrProf, never restored), thermal 0x1A, fan 0x2E manual + 0x27 max (restored on graceful shutdown). **16-step HIL passed on-device** incl. manual fan held past the 120 s clawback window (heartbeat proof), taskkill /F → firmware clawback at ~150 s (AR-12 proven), hysteresis force/release, and the negative ladder (clamp / non-multiple / no-temp-feed / unelevated). On-device corrections M2 surfaced: **0x64F is not in the signed IntelMSR allow-list** (metric removed); the hysteresis release must write 0x27-off **before** re-applying manual levels or the keepalive re-asserts a stale max_fan (HIL-13 catch); max→manual fan ramp-down is ~6–9 s; 8BAB does idle fan-stop (auto = 0 RPM when cold). M3 delivered (the **power-limits special**, first Experimental-gated writes): **PERFEPP1 full chain** (E-core EPP, GUID `…c6864`, mirrors PERFEPP; §32 order EPP→EPP1→max-freq→boost; telemetry `cpu.epp1_ac/dc`), **0x22 GPU platform policy write** (read-modify-write merging unset fields from the live 0x21 readback; verify = delayed 0x21 poll; shutdown restores the startup value when dirty; safety allows `slowdown_temp_c == 0` — 8BAB reports "no knob" and writing 0 back is preserve, HIL-2 catch), and **0x29 CPU power limits behind a permanent Experimental double gate** (cargo feature `experimental-hp-power-limits` + `caps.power_limits == Experimental`; pl4/cc ≠0 rejected — 0xFF = per-byte NO_CHANGE is the only wire value). **The §25 byte-order conflict is SETTLED on-device (spike S2, double A/B): 8BAB firmware wants byte0=PL2, byte1=PL1 — the OPPOSITE of the kernel struct**; effect is immediate; `{0,0,FF,FF}` DEFAULT restore does NOT take effect → coordinator captures the 0x610 baseline before the first write and shutdown restores it explicitly. Verification rides the telemetry feed (`ThermalFeed::power_limits_w()` reads the 0x610 snapshot — the coordinator never touches PawnIO); keepalive gained `ReAssert::PowerLimits` (dirty-flag-driven). **8-step HIL passed**: EPP1 cross-checked vs powercfg /QH; cTGP-off behavioral evidence (59.4 W vs 64.4 W ceiling); the S2 arbitration; three-step full chain (PL1=45 clamped a 32-thread load to a 44 W mean for 160 s sustained; PL2=90 capped turbo at ~90–100 W vs the 108 W default peak); AC/DC transitions left 0x610 flat at the written values for 300 s (Balanced mode — the kernel's victus re-actualization is Performance-mode-specific; no 0x29 clawback exists here); restore to 55/130 journaled rc=0; negatives all rejected pre-engine; default builds have no `power-limits` subcommand at all. On-device lessons M3 surfaced: **trusting the kernel struct would have written PL1=90/PL2=45** (pl2<pl1 — a state the kernel itself forbids; §25's mandatory three-step method exists for exactly this); PowerShell jobs/runspaces decay mid-run as load generators (the `cpu-load` Rust spinner subcommand replaced them); 0x29 stays Experimental forever (pl4/cc unverified, long-term firmware behavior uncharacterized). Deferred to M4+: MUX write (needs reboot), profiles, fan curves, stable UI for 0x22/0x29.

M4-mini/M4.1 delivered (the **PL4 special**, user principle: the gate is *verification*, not a judgment of usefulness — "精细控制意味着我可以不用，但是我知道我能操纵"): **MCHBAR readback channel settled on-device** (signed IntelMCHBAR PawnIO module — source-verified NO write ioctls; AR-09 amended to "read-only signed-module infrastructure + per-op allow-lists", `write_mchbar` on the forbidden list; base 0xFEDC0000; **0x5938 = RAPL power-unit anchor** = MSR 0x606 exact match; **0x59B0 = PL4 register** bits 14:0 × unit; "0x59A0 = 0x610 mirror" literature assumption disproven empirically; dev commands `mchbar-probe` read-only + `pl4-spike` downward-only, the latter proving 0xFF NO_CHANGE on bytes 0/1 by isolation), then **0x29 byte2 entered the control plane**: encoder byte2 = pl4_w (0 = NO_CHANGE; cc still permanently rejected), safety envelope 30..=200 (ceiling = SDD byte5 factory value — a protection limit is never raised above factory), telemetry `cpu.pl4_w` (250 ms, second module on the PawnIO collector; missing module = absent metric, never provider flapping), coordinator verification upgraded to **dual-channel convergence** (0x610 + 0x59B0; pl4 only judged when requested, missing MCHBAR channel → honest Failed, never a silent byte2 skip), baseline capture includes pl4 (0 = unmeasured → restore keeps byte2 NO_CHANGE: never write back a field never measured), keepalive re-asserts carry byte2 via ObservedState, shutdown restores all three explicitly. **HIL passed**: triple write 45/90/150 Verified in ~1 s, keepalive re-assert at 61 s, graceful restore → telemetry 55/130/200; `--pl4 29/201` rejected pre-engine. Side observation (mechanism uncharacterized, recorded honestly): with no custom limits held, this machine's 0x610 drifted 55/130 → 90/90 for ~20 min and relaxed on its own — the capture/restore/re-assert design exists for exactly this; never hardcode 55/130. PL4 evidence tier: write+readback VERIFIED; **transient clamp behavior unverified** (μs-scale, beyond 250 ms sampling) — one reason 0x29 keeps its permanent Experimental gate.

M5 delivered (**profiles — §36 landed, Phase 4 pulled forward at the core+CLI layer**; the UI-level preset switching it described remains Phase 3+): `ApplyProfile{name}` is a name-only command the coordinator **expands** into an ordered concrete plan — PPM → 0x29 → 0x22 (merged over the live 0x21 readback) → thermal → **fan LAST** (fan manual/max suspends the firmware thermal curve, so everything else must be in place first) — then **validates ALL steps upfront** through the existing per-command `safety.validate()` (any failure rejects the whole profile with zero hardware trace, AR-11) and executes in order: first-step failure = Rejected, later-step failure = Partial (no rollback), aggregate verification = weakest step. A successful apply stamps `DesiredState.profile = Some(name)`; any direct Set* command clears it again. `ProfileRegistry` = 4 built-ins (stable-clean by construction — no experimental fields; values from on-device evidence: **silent** = epp/epp1 80/95 + EfficientAggressive boost + firmware-auto fan (M2 HIL-8 idle fan-stop), **balanced** = 出厂参考值一键回原厂 (epp/epp1 0/0, max-freq 0/0, boost aggressive, ctgp/ppab on), **gaming** = Performance thermal + EPP 0, **cpu-max** = Performance + max fan) plus user TOML at `%LOCALAPPDATA%\phelper\profiles\*.toml` (filename = name; a user file shadowing a built-in is skipped with a warning; broken TOML warns and skips, never panics; user files MAY carry `power_limits` — the apply-time double gate decides, never the loader). **M5 HIL catch (BIOS F.30): 0x22 `dstate` writes are INEFFECTIVE on 8BAB** — rc=0 but the 0x21 readback never moves (tested idle and under CUDA load), and the 0x21 dstate readback is itself dynamic (1↔3, same day, same BIOS, uncorrelated with AC/DC or GPU load; mechanism unknown). M3 missed this because the written dstate always happened to equal the readback then; built-ins must not carry dstate (a knob that can't verify can't be preset), ctgp/ppab unaffected. Also newly characterized: `EfficientAggressive` boost (0x04) is writable+readable on 13900HX (powercfg /QH cross-checked), and the journal proves the machine moved **BIOS F.21 → F.30** between M2 and M3 — all M3+ facts are F.30 facts. **HIL passed**: silent cross-checked vs powercfg /QH (EPP 0x50/0x5F, boost 0x04), balanced green after the dstate fix, cpu-max held 70 s past the 60 s heartbeat (safety ForceMaxFan re-stamp at ≥90 °C working as designed), gaming green, three negatives rejected pre-engine, typo'd TOML warned+skipped, and an experimental `power_limits` user profile applied end-to-end Verified (~1.1 s) then restored; the machine ended back at stock (55/130/200, EPP 0/0). TOML trap recorded: toml-rs rejects value-after-table — `PerformanceProfile` field order is scalars first, tables last.

M6 delivered (**GPUI shell v0.1 — §58 Phase 3 landed**, 8BAB on-device verified): the Application layer lives in `phelper-core/src/app/` (pure, zero gpui dep — read-only `AppState` + reducers, per-knob latest-wins `Coalescer` with 250 ms min-interval + Busy backoff (§44's designated home), `AppHandle`/"app-pump" thread owning the Engine, incremental cross-process journal tailer, UiSettings TOML, client validation mirroring the CLI envelopes — all unit-tested), and the UI in `apps/desktop` (bin crate, `test = false`, zh-CN, dark-default): 7 pages (Dashboard / Performance / Thermals / Profiles 色卡 / Monitor 41-metric ownership map / Diagnostics + live journal + JSON report export / Settings) + the experimental drawer (0x29 PL1/PL2/PL4 + 0x22 cTGP/PPAB, compile-time feature && runtime-capability double gate, hidden entirely in stable builds; dstate read-only with the M5 "写入无效" annotation). Self-elevation via runas (NOT requireAdministrator — gpui.lib owns the manifest slot; CLI stays asInvoker), minimize-to-tray (tray-icon crate leaked on the GPUI main thread — Rc-backed, not Send; menu 显示主窗口/退出， 退出 = the same AR-12 graceful path as window close), named-mutex single-instance guard (two instances = two ControlCoordinators keepalive-fighting the same fans — fail closed, AR-11). gpui + gpui-component pinned to matched revs (`cargo tree -i gpui` = exactly one; Cargo.lock committed). **Custom div toggle replaces gpui-component's Switch** (verified bug at the pinned rev: a programmatically-checked Switch paints the checked track but its keyed thumb spring never adopts the new target — two id-keying workarounds failed; plain divs are geometry-as-data re-rendered every tick). **18-item HIL passed on-device**: cold start 0.74 s; coalescing (6-move drag → exactly 2 journal entries, latest-wins Verified); silent/balanced applies cross-checked vs powercfg /QH (0x50/0x5F/0x04 ↔ 0/0/aggressive; step order PPM→thermal→fan visible in the expandable journal evidence); manual fan held 175 s past the clawback window then window-close → shutdown journal + fan auto in ~1 s (one ANOMALY: a single close took ~38 s once — bounded by the 40 s UI-side shutdown timeout, restore completed+journaled correctly, root cause not isolated (suspected transient WMI/provider stall under the test harness's own process spam), unreproduced in 2 clean runs at 0.5–1.2 s); taskkill /F on the app process → firmware clawback reclaims the fan (~150 s, AR-12 regression on the UI process, live-traced: held 2000/2300 → auto curve); safety ForceMaxFan→ReleaseTo hysteresis fired live during HIL (≥90 °C under compile load); the same hysteresis also produced an accidental negative test (manual fan rejected while max-fan held — "max fan is on; return to automatic before manual levels"); stable build (`--no-default-features`) hides the 0x29 card while 0x22 stays; provider-broken run (PHELPER_INTELMSR → garbage file) shows pawnio unavailable/stale honestly; Diagnostics JSON export validated; live journal cross-process ≤2 s; Monitor 41/41 metrics fresh. Resource budget measured on the **release** build (debug numbers are meaningless for this): 8-min idle soak, flat, no leak — Dashboard 7.1% CPU mean / RAM 196–207 MB, Diagnostics 7.9% / 206–215 MB. **The plan's <1%-CPU aspiration is NOT met by the v0.1 render model**: 250 ms tick → full `AppState` deep clone (journal_tail 200 entries + evidence 64) → full element-tree rebuild → DX11 paint, 4×/s with zero memoization — ~17 ms/frame is the design's honest cost, not a hotspot (an earlier "linear CPU climb to 204%" scare was a total-CPU-**seconds** measurement artifact — always compute deltas from `$p.CPU`). The v0.2 pass (below) executed exactly that package. RAM slow-creep +19 MB/8 min recorded honestly (suspect text-shaping cache + allocator arenas, unverified). Deferred: real .ico asset (runtime-drawn placeholder).

v0.2 delivered (**render-performance pass**, 8BAB on-device verified): five changes, all in the Application/UI layer, zero control-path touch. (a) `AppState.journal_tail`/`evidence` are `Arc<VecDeque>` — the 250 ms tick's snapshot clone is now pointer bumps, reducers pay one `Arc::make_mut` per real change. (b) `ChartCache` (RefCell, 1 Hz) feeds the trend charts — v0.1 re-pulled history+downsampled at tick rate, 4× the designed cadence. (c) Diagnostics journal renders ≤50 rows + 显示更多 (200 full rows with per-row listeners was the page's heaviest element cost). (d) **per-page visual fingerprint** (`apps/desktop/src/fingerprint.rs`): one u64/tick over everything the CURRENT page could paint differently; the ticker skips `cx.notify()` while it's unchanged, with a 5 s forced-refresh backstop that fails OPEN toward painting. Bucket pages (Dashboard/Thermals/Monitor/Diagnostics) fold a 1 s time bucket into the hash — **noisy values (MHz, RPM, 0.1 °C) are deliberately NOT hashed: quantizing them at display precision still flips every tick, which defeats the skip entirely (first v0.2 soak caught exactly this — 4.4% instead of the expected ~2%); the bucket IS their display cadence**, only structural changes (quality/source/stale, observed, knobs, journal) repaint between boundaries. Static pages (Performance/Profiles/Settings) hash their few self-moving values and repaint only on real change. Interactive events notify themselves (verified audit: every handler either calls `cx.notify()` or is dispatch-only; slider drags + OS-theme flips gained explicit notifies — v0.1 masked their absence via the unconditional tick). (e) pump/shutdown stage-timing probes: pump iterations >2 s log per-stage durations, `Engine::shutdown()` logs control/telemetry/hp stage timings, the UI logs shutdown begin/end — the M6 38 s-close anomaly now self-reports (live evidence: graceful closes logged control 41 ms / total UI-side 77–144 ms). **Re-measured on release (same delta-from-`$p.CPU` methodology): Dashboard 7.1% → 2.7% CPU mean, Diagnostics 7.9% → 2.9%, RAM 181–184 MB flat over 4 min (creep much slower than v0.1's +19 MB/8 min — fewer frames, less shaping churn). The <1% aspiration is still NOT met** — residual ≈1 Hz repaint cost (~17 ms) + tick hash + pump loop + GPUI/DX11 idle overhead; the next lever would be replacing the 250 ms pull-tick with pump→UI push (no tick at all on static pages), not attempted here. 136+3 tests green, clippy clean, machine ended at factory state.

The former workload/frame-telemetry experiment was removed from the product scope; the telemetry registry now contains only hardware and Windows system metrics.

2026-08-27 responsiveness pass: the existing control path was measured against the reference machine's journal (EPP/Boost execution is typically ~80–130 ms). The app pump now drains telemetry non-blocking and waits on the UI channel, so a control request wakes it immediately instead of waiting behind the old 100 ms telemetry timeout; while a command/pending drag is active, outcome and next-dispatch polling use a 10 ms cadence. CPU-policy knobs use a 50 ms coalescing interval; firmware-sensitive knobs retain 250 ms. The desktop state ticker is 50 ms so the lifecycle badge does not wait a quarter second to reflect the outcome. No safety gate, single-writer rule, firmware verification, or fan 1 Hz rule was weakened.

2026-08-30 minimal-UI reduction: the desktop user surface now has only Dashboard and Profiles. Performance controls are expressed through profiles; Monitor, Settings/theme, application scheduling, tray, autostart, OMEN-key integration, overlay, diagnostics, and report export are not part of the current desktop product. Core/CLI facilities remain where they have an independent engineering use.

Current scope decision: MUX graphics-mode switching is explicitly deferred/out of scope. The reference machine can report MUX capability/current mode, but switching requires a reboot and does not improve the current hot-reload performance-control loop. Keep the read-only probe/state model for support diagnostics; do not add a MUX write control, UI control, or profile field without a separate reboot/rollback/HIL plan.

**`architecture.md` is the source of truth** (post-M0 corrections applied; where it conflicts with older external docs, `docs/feasibility-16-wf0032TX.md` wins). Before writing code, read it — especially §4 (AR-01–AR-12), §21 (MOF facts), §33.1 (KeepAliveService), and §60 (review checklist). New features must be checked against the §60 checklist; if its 15 questions can't be answered, the feature does not go in.

## What this project is

An HP OMEN laptop performance-control and hardware-telemetry desktop app: **Rust + GPUI (gpui-component), Windows 11**, single-process modular monolith. The one and only target: **OMEN Gaming Laptop 16-wf0032TX (SKU 81L09PA, board 8BAB)** — i9-13900HX + RTX 4060 Laptop (AD107). It deliberately reimplements only the performance/telemetry core of OMEN Gaming Hub — no store, RGB editor, cloud, or account features (§3 Non-Goals).

## Hard architectural invariants (§4)

These are non-negotiable; code that violates them is wrong:

- **UI never touches hardware.** GPUI dispatches `ControlCommand`s and subscribes to AppState only — no WMI/PowrProf/NVAPI/PawnIO/MSR/EC calls from UI code.
- **Read path and write path are fully separate.** Telemetry answers "what is happening"; control answers "what we want". Separate data structures, lifecycles, error models, thread models.
- **All hardware/firmware performance writes go through `ControlCoordinator`** (single-writer, serialized FIFO queue). No shortcut HP WMI/PPM write path from any subsystem, CLI, or profile. Process-scoped Windows scheduling is a separate OS-policy surface with its own ownership and restoration lifecycle.
- **Capabilities are discovered, never assumed** (AR-05), and **unknown means unsupported** (AR-06). No feature inference from "OMEN + Intel + NVIDIA".
- **No direct EC writes in supported control paths** (AR-07). EC is at most optional, read-only, board-verified diagnostics behind an `experimental-ec` Cargo feature.
- **Windows CPU policy via PowrProf/PPM APIs only** (AR-08) — never `powercfg.exe` as a backend (dev-verification/diagnostics only), never dual-write HWP policy via MSR.
- **PawnIO is read-only telemetry infrastructure** (AR-09): signed modules + per-operation allow-lists — IntelMSR (thermals, RAPL energy, APERF/MPERF) and IntelMCHBAR (MCHBAR window reads for the PL4 readback probe; that module exports NO write ioctls at all, source-verified). Never expose generic `write_msr()`/`write_io_port()`/`write_ec()`/`write_mchbar()`.
- **Three states stay distinct** (AR-10): `DesiredState` (user intent), `ObservedState` (verified readback), `TelemetryState` (live sensor data). "API returned success" ≠ "hardware is in desired state" — every write needs readback/verification.
- **Fail closed** (AR-11): when safety/support/verification is uncertain, don't write.
- **Firmware safe state wins** (AR-12): on crash/timeout/exit, restore firmware automatic fan control. The app must never be the sole thing keeping the machine cool.

## Structure and dependency direction

Actual workspace (the §6 crate split is realized as modules inside `phelper-core`; splitting further is a mechanical later step):

```
crates/phelper-domain  → pure domain model + ports/traits; deps only serde/thiserror
                         (compiler-enforced AR-01: NO Win32/WMI/GPUI/NVAPI)
crates/phelper-core    → engine + platform adapters behind domain ports:
                         platform/{hp_wmi, pawnio, nvidia, windows_ppm, identity, elevation},
                         capability (probe + BoardProfile boards/8bab.toml),
                         telemetry (M1), control (M2), persistence, read-only smoke probes
crates/phelper-cli     → probe/telemetry dev harness (clap); asInvoker manifest
apps/desktop           → minimal GPUI shell (Dashboard + Profiles; self-elevating, tests enabled)
```

`phelper-domain` must not depend on UI or platform crates; platform modules implement its ports (hexagonal style). `BoardProfile` (developer-maintained: what this machine is/what's verified) and `PerformanceProfile` (user-facing: desired behavior) are permanently separate concepts (§36).

## Domain essentials

- **Metric ownership** (§12): every metric has exactly one authoritative source + declared fallbacks — CPU package temp/power = PawnIO MSR/RAPL; GPU power = **NVML** (fallback ClientPowerTopology, verified 0-entries on this machine); other GPU metrics = NVAPI; RAM/disk/network = Windows; fan/thermal mode = HP WMI. Two collectors never both own a metric.
- **HP WMI control surface** (§21–27): `root\wmi` class `hpqBIntM`, methods `hpqBIOSInt{0,4,128,1024,4096}(InData, OutData)->Boolean`, gaming command group `0x20008`, behind a `BiosInvoker` seam (pub(crate) `raw_execute`; no raw payload escapes the module). Thermal mode on 8BAB is **static V1** ({Balanced=0x30, Performance=0x31}; Cool unconfirmed → not in the writable enum). `0x29` power limits: **byte order settled on-device (byte0=PL2, byte1=PL1 — opposite the kernel struct, §25)**; permanent Experimental gate + cargo feature, three-step verification (0x610 readback + RAPL workload response) passed M3.
- **Windows PPM is a first-class software control surface** (§18/§19): EPP/EPP1, frequency ceiling, processor-performance floor/ceiling, and Boost are independent AC/DC `PowrProf` indexes; EPP is a preference, bounds are constraints, and PL1/PL2/PL4 remain a separate HP hardware envelope.
- **Structured `ControlError`** (§34): UI shows human messages (`Unsupported`, `UnsafeRequest`, `VerificationFailed`, …), never raw HRESULTs.
- **Source reliability tiers** (§54): Tier A (cross-validated incl. Linux `hp-wmi`) → stable; Tier B → board-gated; Tier C → diagnostics/experimental only; **Tier D (unknown) → write prohibited**.
- **Safe rollout for any new hardware write** (§57): read-only probe → dev-only command → reference-machine verification → capability-gated experimental UI → stable. Never "found a register on Monday, in stable UI on Tuesday".
- **Sampling** (§38): per-domain cadences; HP WMI/firmware is *never* high-frequency polled (~1 s slow sensors).

## Licensing constraint (§55)

Key protocol references (OmenSuperHub, Linux `hp-wmi`, PawnIO) are **GPL**. Re-implement adapters from protocol behavior and public documentation — do not copy GPL code into this project. The NVAPI SDK is safe to use directly under its own terms.

## Implementation order (§58)

Phase 0 is a **CLI hardware probe** (DeviceIdentity, Board ID, BIOS, ThermalPolicyVersion, SystemDesignData, WMI availability, NVIDIA, PawnIO, Windows PPM) to build the reference-platform capability snapshot — before any GPUI work. Then: telemetry foundation → control foundation (CLI/integration-tested) → GPUI shell (**done, M6**) → profiles (**done, M5 core+CLI / M6 UI**) → Windows PPM fine policy (**done**) → advanced thermal.

## Build / test commands

Standard Cargo workspace:

- `cargo build --workspace` / `cargo test --workspace` / `cargo clippy --workspace` (all clean; current all-feature workspace run: 168 tests)
- `cargo run -p phelper-desktop` — the minimal GPUI shell. **Self-elevates via runas** (UAC prompt on unelevated launch) and enforces a named-mutex single instance. It has no product feature flags and contains only Dashboard + Profiles. Resource/perf measurements must use `--release` (debug GPUI numbers are meaningless). Logs: `%LOCALAPPDATA%\phelper\logs\phelper-desktop.log`; journal: `%LOCALAPPDATA%\phelper\state\control-journal.jsonl`.
- `cargo run -p phelper-cli -- probe [--json PATH] [--record-fixtures DIR] [--emit-board-profile PATH]` — read-only capability probe. **Must run elevated** (`root\wmi` ACL is admin-only → 0x80041003 otherwise); unelevated it degrades to identity-only.
- `cargo run -p phelper-cli -- telemetry [--interval-ms N] [--duration S] [--metrics SUBSTR]...` — live metric table (repeat `--metrics` per substring). Ctrl+C now shuts the engine down GRACEFULLY (the engine includes the control coordinator since M2 — an ungraceful kill leaves fan/thermal state to the ~120 s firmware clawback).
- `cargo run -p phelper-cli -- control <SUB>` — M2 control plane (hard-enables `phelper-core/control`). `status` (caps + observed + Windows PPM snapshot + OGH findings + journal tail), `epp --ac N --dc N`, `epp1 --ac N --dc N`, `max-freq --ac MHZ --dc MHZ` (0 = unlimited), `min-perf --ac N --dc N`, `max-perf --ac N --dc N`, and `boost [MODE] [--ac MODE] [--dc MODE]` (AC/DC omitted side stays unchanged). `thermal <balanced|performance> [--hold 120]`, `fan auto`, `fan max --on|--off [--hold 120]`, `fan manual --cpu RPM --gpu RPM [--hold 120]` (RPM must be multiples of 100 — rejected client-side before engine start). M3 added: `gpu-policy [--ctgp on|off] [--ppab on|off] [--dstate 1..4] [--slowdown-temp C] [--hold 120]` (0x22 read-modify-write — unset fields merge from a fresh live 0x21 readback at execution time; **M5 HIL warning: `--dstate` writes are ineffective on 8BAB — rc=0 but the 0x21 readback never moves; the help text says so**), and — only in `--features experimental` builds — `power-limits --pl1 W --pl2 W [--pl4 W] [--hold 120]`. M5 added profile list/show/export/apply. Every mutating command prints §56 BEFORE/COMMAND/AFTER. HP-state commands hold the process so the 60 s heartbeat keeps the firmware from clawing state back; Ctrl+C/Break or hold expiry → graceful restore (AR-12). A zero hold is rejected before engine startup. Windows PPM settings persist after process exit and are never auto-restored; configured/effective Windows modes are read-only context.
- `cargo run -p phelper-cli -- gpu-load [--seconds N] [--mem-mib N]` — DEV-ONLY verification load generator (CUDA Driver API via nvcuda.dll, no toolkit needed). Wakes the dGPU so gpu.power_w / clocks can be acceptance-tested. Never touches write paths.
- `cargo run -p phelper-cli -- cpu-load [--seconds N] [--threads N]` — DEV-ONLY CPU spinner (M3; replaced PowerShell jobs/runspaces, which decay mid-run and corrupted early HIL-4 samples). Pure userspace, zero hardware access.
- `cargo run -p phelper-cli -- mchbar-probe` — DEV-ONLY (§57 Stage 1, M4-mini) READ-ONLY MCHBAR cross-check (zero writes): IntelMCHBAR module load + base sanity, MMIO 0x59A0 vs MSR 0x610, SA power-block sweep for the factory PL4 (SDD byte5 = 200 W). On 8BAB it found: base 0xFEDC0000, RAPL power-unit anchor at +0x5938, PL4 register at **+0x59B0** (0x59A0 is NOT the 0x610 mirror on this platform — the layout differs, found empirically).
- The CLI manifest is `asInvoker` deliberately: elevation is the operator's choice (run from an admin terminal), the engine detects the token and degrades (unelevated: pawnio + hp-wmi unavailable, telemetry continues, writes → `PermissionDenied`). `highestAvailable` breaks `cargo test` from non-elevated shells (os error 740/741).
- `assets/pawnio/IntelMSR.bin` + `IntelMCHBAR.bin` + `COPYING` — signed PawnIO modules (LGPL, from namazso/PawnIO.Modules release 0.2.10). The module bytes and license notice are compile-time embedded in the exe; the final desktop build has no PawnIO asset-file dependency. Note: **MSR 0x64F (limit reasons) is NOT in the IntelMSR allow-list** (0x80070005 on-device) — that metric was removed; 0x610 PL1/PL2 works.
- `docs/hil*.ps1` — the M2 HIL scripts (fan hold / clawback / hysteresis / watchdog gate), kept as re-runnable verification assets.

Hardware-in-the-loop and verification tests (before/command/after, e.g. WMI `0x29` write + MSR readback + RAPL workload response) are required for control features and must run on the physical reference machine (§56).

M7 fan ownership correction: the desktop UI no longer exposes `FirmwareAuto` as
“自动”. `0x2E {0,0}` is a firmware handoff used only for shutdown, watchdog,
and failed-write recovery; the built-in `silent`, `balanced`, and `gaming`
profiles now own a positive software fan curve. Switching from max fan to
manual/curve releases `0x27` and immediately writes the requested software
target, so the UI has no hidden “return to automatic” step.

M8 fan session safety: shutdown restoration is ownership-aware. A session that
never successfully wrote fan control does not send `0x2E {0,0}` or `0x27 off`;
fan/thermal restore writes are emitted only for domains changed by this
session. The last successfully applied software curve is persisted at
`%LOCALAPPDATA%\phelper\state\fan_curve.toml` for editing on the next start;
loading it never auto-takes control, and no firmware temperature curve
readback is claimed.

Current Windows PPM fine-policy pass: the software layer now models the active
PowrProf scheme separately from Windows 11's configured mode and effective mode.
EPP/EPP1, maximum frequency, minimum/maximum processor performance, and Boost
are read and written as AC/DC-specific PPM indexes with immediate readback;
configured/effective modes are read-only context and are never silently switched.
Capability probing reuses the startup snapshot, and the UI keeps bounds behind
“更多参数” so the main page remains compact. `control status` exposes the full
snapshot. Core has 163 tests plus 6 domain tests green with all features; see
`docs/windows-power-policy.md` for the design boundary and official API links.
